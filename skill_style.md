# AI 教学视频剪辑 · 风格说明书

> 驾考教学视频自动剪辑，适配科三路考 / 科目二 / 日常练车等多种场景。
> 核心原则：**风格规则是通用的，路线图是每次视频自带的，VLM 动态做位置匹配。**

---

## 整体架构

```
新视频 + 路线图(可选) + 特写素材(可选)
              │
      ┌───────┴────────┐
      │  VLM 帧条件检测  │  ← 判断: 有文字？有语音？有路线图？
      └───────┬────────┘
              │
   ┌──────────┼──────────┐
   ▼          ▼          ▼          ▼
 分支A      分支B      分支C      分支D
 文字+地图  语音+地图  纯视觉     无地图
 (最准)    (较准)    (粗略)    (仅通用)
              │
              ▼
      MoviePy 渲染 → output/成片.mp4
```

---

## 条件检测（每段视频帧自动执行）

VLM 分析每一段的关键帧，输出：

| 检测项 | 含义 | 影响 |
|---|---|---|
| `has_text_overlay` | 画面上是否有叠加的文字标签 | 决定能否走分支A（最准） |
| `has_valid_speech` | ASR 是否检测到有效人声 | 决定能否走分支B（ASR辅助） |
| `has_route_map_in_frame` | 是否提供了路线图素材 | 决定是否叠加路线图和红点 |
| `location` | 车内 / 车外道路 / 考场场地 / 驾校场地 | 影响特效选择 |
| `activity` | 讲解 / 实操 / 起步 / 停车 / 转向 / 考试模拟 | 触发不同风格规则 |

---

## 四条处理分支

### 分支A: 有文字标签 + 有路线图（精度最高，~80%）

**触发条件**: `has_text_overlay = true`, `has_route_map_in_frame = true`

**处理流程**:
1. VLM 读取帧上的文字标签（如 "前方路口右转"、"靠边停车"）
2. VLM 对照路线图图片，找到文字对应的地理位置
3. 输出红点的 (x, y) 比例坐标
4. 红点在各坐标之间插值动画，跟随路线移动

**适用场景**: 用户已剪辑过的视频（自带文字标签），二次精加工

**红点动画**: 坐标序列之间 ease_in_out 插值，不匀速不跳跃

---

### 分支B: 有语音 + 有路线图，无文字标签（精度中等，~60%）

**触发条件**: `has_valid_speech = true`, `has_text_overlay = false`, `has_route_map_in_frame = true`

**处理流程**:
1. 从 ASR 转写中提取位置关键词（起步、路口、红绿灯、转弯、掉头、变道、超车、停车、终点、斑马线、公交站、学校）
2. VLM 根据关键词在路线图上估算对应位置
3. 红点在匹配到的坐标之间匀速移动

**适用场景**: 原始教学视频（有人声讲解但无文字标签）

**红点动画**: 段间匀速线性插值

---

### 分支C: 无文字 + 无语音 + 有路线图（精度较低，~40%）

**触发条件**: `has_text_overlay = false`, `has_valid_speech = false`, `has_route_map_in_frame = true`

**处理流程**:
1. VLM 对比路面实景特征（道路弯曲形态、标线、路口形状）与路线图几何结构
2. 做视觉空间匹配，估算车辆在路线图上的大致位置
3. 红点整段匀速移动

**适用场景**: 纯驾驶画面、环境音、无讲解的实操片段

**红点动画**: 段内匀速，段间不插值

---

### 分支D: 无路线图（纯通用风格）

**触发条件**: `has_route_map_in_frame = false`（input/ 下无路线图素材）

**处理流程**:
- 不叠加路线图，不做红点动画
- 应用通用规则：zoom 缩放、画中画、字幕烧录

---

## 通用风格规则（所有分支都会应用）

### RULE_ZOOM: 实操段缓慢推进

| 触发条件 | 动作 |
|---|---|
| `activity = 实操演示` | scale 1.0 → 1.12 缓慢 zoom in，增强临场感 |

### RULE_PIP: 关键操作画中画

| 触发条件 | 动作 | 素材位置 |
|---|---|---|
| `activity = 起步操作 或 停车操作` | 叠加脚部操作特写 | 左下角 (scale 0.25) |
| `activity = 讲解灯光` | 叠加仪表盘/灯光特写 | 左上角 (scale 0.25) |
| 画面有路线图需求 | 叠加路线图 | **右上角** (scale 0.35) |

**布局规则**: 路线图始终右上角，特写素材左上角或左下角，**两者永不重叠**。

**素材可选**: 如果 `input/` 下没有对应的特写素材文件，自动跳过该画中画。

### RULE_TEXT_OVERLAY: 字幕烧录

| 触发条件 | 动作 |
|---|---|
| `has_valid_speech = true` | ASR 文本 → 底部居中字幕，白色字体 + 黑色描边 |

---

## 路线图素材处理

1. **输入**: 用户提供的路线图（可能有背景杂讯）
2. **预处理**: 蓝色色度抠图 → 透明背景 → 仅保留蓝色路线路径
3. **位置**: 叠加在视频**右上角**，scale 约 0.35，不透明度 0.9
4. **红点**: 8-12px 红色圆点，在路线路径上移动

---

## 输入目录结构

```
input/
├── video.mp4              ← 必填: 要处理的视频
├── route_map.png          ← 可选: 本期视频的路线图（有就放）
├── foot_closeup.mp4/jpg   ← 可选: 脚部操作特写素材
└── dashboard_closeup.mp4/jpg ← 可选: 仪表盘/灯光特写素材
```

---

## 扩展其他视频类型

当处理非科三视频（如科目二、日常练车）时：

1. VLM 第一步自动判断视频类型
2. `activity` 标签自动扩展（如 `倒车入库`、`侧方停车`、`坡道起步`）
3. 通用规则（zoom、PiP、字幕）不受影响
4. 路线图相关规则只在有路线图时才触发

**不需要修改引擎代码**，风格规则自动适配。
