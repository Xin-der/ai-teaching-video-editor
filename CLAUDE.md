# CLAUDE.md — 项目协作规则

## Git 提交

- **提交消息末尾不要加 `Co-Authored-By: Claude` 署名行**。提交身份始终是项目自己的 git 用户（Xin-der）。
- 只有用户明确要求时才 `git push`。

## 项目状态

- 项目状态/方向/待办看 **HANDOFF.md** 和 **PROGRESS.md**，开工前先读。
- 协作尽量轻量直接：能按现有文档实现的直接写，拿不准的方向开头一次性确认。
