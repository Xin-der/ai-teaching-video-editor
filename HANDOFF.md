# 项目交接文档

> 2026-07-28 | 方向调整：全自动出片 → 多平台智能切片工具

---

## 一句话概括

从这个方向：**AI 学你的剪辑风格 → 自动渲染完整 mp4**
改到这个方向：**长教学视频 → AI 理解内容 → 智能切片 → 多平台适配导出（抖音/B站/小红书）+ 文案生成**

---

## 为什么改方向

1. 旧方案试图让 AI 模拟剪辑师的审美判断（路线图叠加、红点跟随、关键帧动画），技术上极难做到准确
2. 全流程硬编码严重（ASR/VLM/渲染全部写了死路径），第二段出片时完全不读新视频
3. 10 小时渲染跑不出正确结果，体验不可接受
4. 新方案更务实：AI 做擅长的事（理解内容），人工做擅长的（确认分段），模板做重复的（多平台适配）

---

## 新旧方案核心区别

| | 旧方案 | 新方案 |
|---|---|---|
| **AI 做什么** | 猜剪辑审美 + 还原特效参数 | 理解教学内容 + 智能分段 + 评分 |
| **输出** | 一条完整 mp4 | 多个平台版本 + 文案 |
| **用户交互** | 期望全自动 | 预览确认分段 → 一键导出 |
| **渲染** | 模拟剪映全套特效 | 模板式叠加（标题卡+字幕+进度条） |
| **可维护性** | 十几个脚本散落 | 3-4 个核心文件 |

---

## 当前代码状态（清理后）

```
D:\Projects\ai视频剪辑agent\
├── scripts/
│   ├── run_asr.py          ← 保留：FunASR 语音识别
│   └── run_scenes.py       ← 保留：PySceneDetect 场景检测
├── docs/
│   ├── AI视频剪辑方案调研.md    ← 保留：原始调研
│   └── 新方案_多平台切片工具.md  ← 新：完整方案设计
├── ref/                    ← 参考视频和素材（不动）
├── work/                   ← 第一段的中间产物（不动）
├── HANDOFF.md              ← 本文件
├── PROGRESS.md             ← 项目进度
├── README.md               ← 项目说明
├── run.py                  ← 需重写
├── requirements-lock.txt   ← Python 依赖
└── .env                    ← API Key 配置
```

---

## 第一段产出的可用资产

| 资产 | 路径 | 用途 |
|---|---|---|
| ASR 引擎 | `scripts/run_asr.py` | 中文语音转文字，FunASR Paraformer，分段处理 |
| 场景检测 | `scripts/run_scenes.py` | PySceneDetect 自动分镜 |
| VLM 调用模式 | 旧 `describe_frames.py`（已删，但 DashScope 多模态调用模式已验证可用） | qwen3.7-plus 多模态 |
| API 配置 | `.env` | DASHSCOPE_API_KEY, MODEL=qwen3.7-plus |
| 参考视频分析 | `work/asr_result.json`, `work/frame_descriptions.json` | 可作为知识库参考 |

---

## 新方案需要新建的文件（下次会话）

```
新增:
├── engine/
│   ├── pipeline.py         ← 核心管线：音频提取→ASR→分镜→VLM→评分
│   ├── scorer.py           ← 片段评分引擎（关键词+知识库+时间占比）
│   └── exporter.py         ← 多平台导出（模板渲染）
├── templates/
│   ├── douyin.json         ← 抖音模板（9:16竖屏+大字幕+关键词弹窗）
│   ├── bilibili.json       ← B站模板（16:9+知识卡片+章节）
│   └── xiaohongshu.json    ← 小红书模板（1:1+要点覆盖+封面）
├── knowledge/
│   └── driving_exam.json   ← 驾考知识库（扣分点+高频错误+重点话题）
└── run.py                  ← 重写为简单入口
```

---

## 待确认事项（留给下次会话）

1. 是否需要做 Web 预览界面让用户拖拽调整分段？还是先做命令行版本？
2. 知识库内容：驾考扣分规则需要用户提供，还是一开始先不加入评分，纯靠关键词匹配？
3. 是否需要支持除了驾考之外的其他教学类型？还是一开始只聚焦驾考？
